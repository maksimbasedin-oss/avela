<div class="customers">
  <h1>{{ 'customers.reset' | t }}</h1>

  {% form 'reset_customer_password', class: 'customers__form' %}
    {{ form.errors | default_errors }}
    <div class="field">
      <label class="field__label" for="ResetPassword">{{ 'customers.login.password' | t }}</label>
      <input
        id="ResetPassword"
        class="field__input"
        type="password"
        name="customer[password]"
        autocomplete="new-password"
        required
      >
    </div>
    <div class="field">
      <label class="field__label" for="ResetPasswordConfirm">{{ 'customers.password_confirm' | t }}</label>
      <input
        id="ResetPasswordConfirm"
        class="field__input"
        type="password"
        name="customer[password_confirmation]"
        autocomplete="new-password"
        required
      >
    </div>
    <button class="button" type="submit">{{ 'customers.reset' | t }}</button>
  {% endform %}
</div>

